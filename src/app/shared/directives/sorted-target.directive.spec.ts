import { SortedTargetDirective } from './sorted-target.directive';

describe('SortedTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new SortedTargetDirective();
    expect(directive).toBeTruthy();
  });
});
